<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:RodriguezAgendaContactosApp.ViewModels"
             x:Class="RodriguezAgendaContactosApp.Views.NuevoContactoPage"
             Title="Rodrigo Rodriguez - 31/05/1992">

    <StackLayout Padding="20">
        <Entry Placeholder="Nombre" Text="{Binding Nombre}" />
        <Entry Placeholder="Correo" Text="{Binding Correo}" Keyboard="Email" />
        <Entry Placeholder="Teléfono (+593)" Text="{Binding Telefono}" Keyboard="Telephone" />
        <StackLayout Orientation="Horizontal" Margin="0,10,0,0">
            <Label Text="Favorito" VerticalOptions="Center" />
            <Switch IsToggled="{Binding Favorito}"/>
        </StackLayout>
        <Button Text="Guardar" Command="{Binding GuardarCommand}" Margin="0,10,0,0" />
    </StackLayout>
</ContentPage>